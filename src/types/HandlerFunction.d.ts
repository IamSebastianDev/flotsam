/** @format */

/**
 * @type { Function }
 * @description
 * Handler function to pass to the `on` method of Flotsam, used as handler
 * during th emitting of events.
 */
export type HandlerFunction = (...args: any[]) => void;
